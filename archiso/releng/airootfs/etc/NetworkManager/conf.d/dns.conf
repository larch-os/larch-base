# Larch uses a static /etc/resolv.conf (nameserver 1.1.1.1) instead
# of per-connection DNS. dns=none tells NetworkManager not to touch
# resolv.conf at all -- without this, it rewrites the file on every
# connection change, undoing the static config.
[main]
dns=none

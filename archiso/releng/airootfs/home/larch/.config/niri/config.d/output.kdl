// No output blocks: let niri auto-detect the display and pick its
// preferred mode. Hardcoding a connector name (eDP-1, HDMI-A-1, ...) only
// matches specific real hardware and leaves a VM's virtual output (e.g.
// Virtual-1) or unknown live-boot hardware unconfigured.
